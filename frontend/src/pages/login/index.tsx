import LoginForm from "./formulario/form";

export default function LoginPage() {
  return (
    <div className="w-full">
      {/*Toglle já está funcionando, preciso apenas ir alterando a cor*/}
      <LoginForm />
     
    </div>
  )
}